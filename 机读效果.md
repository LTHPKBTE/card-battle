# 机读效果 (YAML) 编写指南

卡牌库每张卡都有「机读效果」字段（卡牌编辑器里那块 YAML 文本）。它是给战斗引擎读的，
引擎不认自然语言，只认这里的结构化描述。

- **唯一的真实来源**：`src/卡牌系统/引擎/schema.ts`（zod 校验）；
  数值公式的语法在 `src/卡牌系统/引擎/值.ts`。本文档若与它们冲突，以代码为准。
- **可执行的范例**：`src/卡牌系统/引擎/测试卡.ts`（22 张卡覆盖各种机制，包括能量费用与 `ask` 询问）。
- **编辑器「插入示例」的内容**：`src/卡牌系统/卡牌/机读效果示例.ts`，
  由 `引擎/测试.ts` 校验，保证永远与引擎语法同步。
- 改引擎语法时请同步本文件，清单见文末 [维护本文件](#维护本文件)。

> 能量（上场费用）的卡库字段与战斗配置见 [能量（上场费用）](#能量上场费用)；
> 需要玩家先点头的效果见 [`ask`](#需要先问一下的效果-ask)。

> 现状：引擎尚未接入面板（AI 与引擎如何交互还没设计），机读效果目前只由人/AI 手写在卡牌上。

## 第一原则：用户说了算

下面所有的数值参考、示例与默认写法，**只在用户没提要求时生效**：

- 用户明确说要什么（强度、机制、风格），就以用户为准；两者冲突时一律听用户的。
- 这不是严格的数值游戏，**也不需要对战平衡**。卡牌是为剧情服务的，不存在「超模」或「破坏平衡」，
  不用为了让两边打得好看而把卡削弱或加强。
- AI 写卡（见下文「不想学这套语法」）同样遵守这条：它不会用「太强了」之类的理由打回你的要求。

## 顶层结构

两种写法都支持：

```yaml
# 多效果
effects:
  - id: 效果1
    on: TURN_END
    operations: [...]
  - id: 效果2
    modifiers: [...]
```

```yaml
# 单效果简写
on: TURN_END
operations: [...]
```

空对象 / 留空 = 这张卡没有机读效果（合法）。

**所有字段都是 `strictObject`：键名写错、缺字会直接报错**，不会静默忽略。

## 一条效果的字段

| 字段 | 说明 |
| --- | --- |
| `id` | 效果标识，省略则按序号自动生成。也用于 `limit` 计数 |
| `on` | 触发时点。**省略 = 常驻效果**，只提供 `modifiers` |
| `condition` | 触发条件，不满足就不发动 |
| `priority` | 结算优先级，数值大的先结算，默认 0 |
| `cost` | 发动代价，任一操作执行不了则整体不发动 |
| `operations` | 效果内容，按顺序执行 |
| `modifiers` | 常驻修正（不触发，只是持续改数值） |
| `limit` | `{ per: TURN \| BATTLE, times: n }` 使用次数限制（同名卡的不同实例各自计数） |
| `when` | 只在谁的回合触发：`CONTROLLER` / `OPPONENT` / `ANY`（默认 `ANY`）。`TURN_START` / `TURN_END` 没有归属方，写它没有意义 |
| `by` | 事件必须由谁发起才触发：`SELF`（默认）/ `ALLY` / `ENEMY` / `ANY` |
| `ask` | `true` = 这条效果发动前先问一下玩家（见 [需要先问一下的效果](#需要先问一下的效果-ask)）。不写 = 照常自动发动 |
| `ask_default` | 没人回答时的默认：`RUN`（默认，照常发动）/ `SKIP`（这次不发动）|
| `zones` | 来源卡要在哪些区域该效果才生效，默认 `['FIELD']` |

⚠️ **效果默认只在来源卡位于场上时生效。** 在别处触发要显式写：

- 抽牌时触发：`zones: ['HAND', 'FIELD']`
- 在墓地生效：`zones: ['GRAVEYARD']`

状态（`APPLY_STATUS`）带来的效果不受此限制，只看状态自己的寿命。

### 触发归属（`by`）

卡上写「攻击后附加纠缠」「上场时……」时，指的是**这张卡自己**的事件；引擎默认就是这个规则：

```yaml
- id: silence
  on: AFTER_ATTACK        # by 省略 = SELF, 只有「缄默」自己的攻击才算
  operations: [...]
```

想要反应型效果（别人做的事也让这张卡跟着发动）就要显式写：

| 值 | 含义 | 典型写法 |
| --- | --- | --- |
| `SELF` | 发起者必须是本效果所属的卡（默认） | 本卡攻击后 / 本卡上场时 / 本卡被抽到时 |
| `ALLY` | 发起者是本卡控制者的卡或该玩家本人 | 我方任意卡攻击后 |
| `ENEMY` | 发起者是对手的卡或对手本人 | 被攻击时反击 |
| `ANY` | 不限制发起者 | 陷阱：我方卡将受到致命伤害时 |

事件的发起者（`GameEvent.actor`）就是事件主角：攻击/伤害事件里是攻击者，`ON_SUMMON` 是上场的卡，
`ON_DRAW` 是被抽到的卡。发起者是玩家的事件（`TURN_START` / `TURN_END` / `ON_RECYCLE`）不做
`SELF` 判断，那些时点的归属用 `when` 控制；`ALLY` / `ENEMY` 则改按发起的是哪一方的玩家来判断。

常驻修正（`modifiers`）不带 `by` —— 它不触发，只持续改数值。

## 触发时点 (`on`)

| 时点 | 时机 |
| --- | --- |
| `MANUAL` | **不由事件触发，等玩家（或 AI）主动发动** |
| `BATTLE_START` | 战斗开始 |
| `TURN_START` | 回合开始（一个新回合的头一件事，回合结算已完成） |
| `TURN_END` | 回合结束（**双方都行动完后**，在回合结算里） |
| `SIDE_START` | 某一方开始行动（`when` 可以区分是哪一方开始） |
| `SIDE_END` | 某一方行动结束（还没有进入回合结算） |
| `SIDE_CHANGE` | 行动方刚换完人（每回合发生 2 次：一方收手、另一方开始时） |
| `BEFORE_ATTACK` | 攻击宣言后、伤害结算前 |
| `AFTER_ATTACK` | 攻击完全结算后 |
| `BEFORE_DAMAGE` | 伤害结算前（可改数值 / 取消） |
| `AFTER_DAMAGE` | 伤害结算后 |
| `BEFORE_HEAL` / `AFTER_HEAL` | 回复前 / 后 |
| `SHIELD_BROKEN` | 某张卡的护盾被打到 0（**边沿触发**：重新生成护盾后再被打空会再次触发） |
| `BEFORE_DESTROY` | 破坏前（可取消） |
| `AFTER_DESTROY` | 破坏后（已进墓地） |
| `ON_SUMMON` | 进入场上 |
| `ON_LEAVE` | 离开场上（区域已改变） |
| `ON_DRAW` | 抽牌后 |
| `ON_RECYCLE` | 牌库抽空、墓地洗回牌库时（**在洗牌之前**派发，所以 `zones: ['GRAVEYARD']` 的效果能捕捉到） |

### 回合是怎么走的

**一个回合 = 先手方行动一次 + 后手方行动一次**，两边各收手之后才进入回合结算。
（面板上的「结束行动」只是把自己这半边交出去，不是结束回合。）

```
回合 N
 ├─ 先手方行动:  SIDE_START → (出牌 / 攻击 / 发动) → SIDE_END → SIDE_CHANGE
 ├─ 后手方行动:  SIDE_START → (出牌 / 攻击 / 发动) → SIDE_END
 └─ 回合结算:    持续伤害/持续加成的寿命 -1 (TURN_END) → TURN_END
回合 N+1: TURN_START → 先手方行动 …
```

- 想写「每次换人时触发」（每回合 2 次）用 `SIDE_CHANGE`。
- `SIDE_START` / `SIDE_END` / `SIDE_CHANGE` 都带归属方（就是正在行动/刚结束的那一方），
  所以「我自己开始行动时」写 `on: SIDE_START` + `when: CONTROLLER`，
  「对手开始行动时」写 `on: SIDE_START` + `when: OPPONENT`。
- `TURN_START` / `TURN_END` 是**回合**的两端，不属于任何一方，所以不带行动方；
  要靠「谁的回合」区分的卡（例如只在对方回合发动的陷阱）用 `when: OPPONENT`。
- **持续伤害/持续加成的寿命在回合结算时才 -1**（`tick_on: TURN_END`，见下文
  [修正层与持续时间](#修正层与持续时间)），不会因为换边而多扣一次。

### 主动发动的效果（`on: MANUAL`）

不会自动触发，等玩家点。引擎侧用这几个函数操作：

| 函数 | 作用 |
| --- | --- |
| `listActivatable(state, player?)` | 列出此刻可发动的效果：`{ card_id, effect_id, def_id, label }[]` |
| `activate(state, card_id, effect_id)` | 发动；`effect_id` 可以传 `listActivatable` 给的 `effect_id`，也可以直接传机读区里的 `id` |

`activate` 返回 `false` 的常见原因：代价付不出、次数用尽、`when` 不匹配、`condition` 不满足。
面板/AI 想展示「能做什么」时应该调 `listActivatable`，不要自己推断条件。

### 需要先问一下的效果 (`ask`)

有些效果不该自己发动 —— 「是否弃一张牌追加伤害」「要不要用掉这张陷阱」这类选择只能玩家来做。
给效果加 `ask: true`，它就会从「到点自动发动」变成「先问一句」：

```yaml
- id: 守夜
  on: AFTER_ATTACK
  ask: true
  ask_default: SKIP        # 一直没人回答就当没发动
  operations:
    - type: MODIFY
      target: SELF
      stat: atk
      layer: ADD
      value: 500
      duration: { turns: 3 }
```

| 字段 | 取值 | 说明 |
| --- | --- | --- |
| `ask` | `true` / 省略 | 省略就是老行为，无需确认 |
| `ask_default` | `RUN`（默认）/ `SKIP` | 回答表里没有它时怎么办 |

怎么回答（引擎侧）：

- **面板**：弹窗里选（弃牌类会直接列出可选卡牌），或者点「都不发动」用它自己的默认值；
- **AI**：在操作里带 `answers: ["效果id"]`（批准）或 `[]`（不发动）；弃牌类用
  `{"do":"discard","cards":["c3"]}`；
- **完全不回答**：`answers` 为 `null` 时按 `ask_default` 处理，并在日志里记一条
  `…的【守夜】这次没有发动 (机读区标了 ask, 需要先确认)`。

弃牌类询问（手牌超上限）不需要在卡上写任何东西：引擎自己会开一条 `DISCARD` 询问，
选项就是你手上那几张牌，不回答就丢掉最便宜的那张。

## 目标 (`target` / `from` / `to`)

固定关键字：

| 值 | 含义 |
| --- | --- |
| `SELF` | 效果来源卡自身 |
| `CONTROLLER` / `OPPONENT` | 效果控制者 / 其对手（玩家） |
| `EVENT_ACTOR` | 当前事件的发起者 |
| `EVENT_TARGET` | 当前事件的目标 |
| `EVENT_SOURCE` | 当前事件的来源卡 |
| `ALL_ALLIES` / `ALL_ENEMIES` / `ALL_FIELD` | 自己 / 对手 / 双方场上的全部卡 |
| `RANDOM_ALLY` / `RANDOM_ENEMY` | 自己 / 对手场上随机一张 |
| `LOOP_ITEM` | **只在 `FOR_EACH` 的循环体里有效**，指当前这一轮处理的那张卡（也可以写 `ITEM`） |

也可以直接指定卡牌实例 id：`target: { id: c12 }`。

或者写查询条件（筛选 + 数量 + 排序）：

```yaml
target:
  zone: FIELD            # DECK / HAND / FIELD / GRAVEYARD / BANISHED
  controller: SELF       # SELF / OPPONENT（控制者）
  owner: SELF            # SELF / OPPONENT（拥有者）
  series: 炎龙
  type: 从者
  name: 愿之芽
  rarity: UR
  has_status: 灼烧
  exclude_self: true     # 排除来源卡自身
  exclude_event_target: true
  max: 2                 # 最多取几个，省略 = 全部
  sort: HP_DESC          # ATK_DESC / ATK_ASC / SHIELD_DESC / SHIELD_ASC / HP_ASC / HP_DESC / RANDOM
```

默认排序是「场上 → 场上位置 → 实例 id」，保证结果确定。

## 数值：常量、公式与聚合

所有「要写数字的地方」（`value`、条件里的 `value`…）都叫**数值**，三种写法等价：

```yaml
value: 100                                              # 1) 常量（老写法）
value: "SELF.atk / 2"                                   # 2) 公式字符串
value: { op: DIV, args: [{ stat: atk, of: SELF }, 2] }   # 3) 结构化表达式
```

公式里能写的东西（名称大小写不敏感）：

| 写法 | 含义 |
| --- | --- |
| `SELF.atk` | 某个目标的某个数值：`atk` / `shield_max` / `shield` / `hp_max` / `hp`；目标可以是任意关键字或 `{ id }` |
| `atk` | 省略目标 = 自己 |
| `$变量名` | 读变量（没写过就是 0） |
| `TURN` | 当前回合数 |
| `EVENT.value` / `EVENT.lethal` | 当前事件的数值 / 是否致命 |
| `RANDOM` | `[0, 1)` 的随机数（按战斗种子，回放可复现） |
| `MAX(a, b)` / `MIN(a, b)` | 取大 / 取小 |
| `SUM(ALL_FIELD, atk)` / `AVG(...)` / `MAX(ALL_FIELD, hp)` / `MIN(...)` | 对一批目标统计（第一个参数是目标关键字，第二个是字段） |
| `COUNT(ALL_ENEMIES)` | 数目标个数 |
| `POW(a, b)` / `ABS(x)` / `FLOOR(x)` / `CEIL(x)` / `ROUND(x)` | 幂 / 绝对值 / 向下取整 / 向上取整 / 四舍五入 |
| `+ - * / % ^` 与括号 | 加减、除、取模、幂；**除以 0 得 0**（不会崩，也不会出 NaN） |

```yaml
value: "SELF.atk * 2"
value: "(TURN + 1) * 100"
value: "MAX(SELF.atk, 500) / 2"
value: "SUM(ALL_FIELD, atk) / 4"
value: "$层数 * 150"
```

⚠️ `MAX(SELF.atk)` 读作「全场最高的攻击力」（聚合），`MAX(SELF.atk, 400)` 读作「取大的那个」（普通运算）：
**只写一个裸目标字段是聚合，写了逗号就是普通运算。**

结构化写法（与公式字符串编译成同一棵树，两种随便挑）：

| 写法 | 含义 |
| --- | --- |
| `{ op: ADD\|SUB\|MUL\|DIV\|MOD\|POW\|MIN\|MAX\|NEG\|ABS\|FLOOR\|CEIL\|ROUND, args: [数值...] }` | 运算 |
| `{ stat: atk, of: 目标 }` | 某目标的数值 |
| `{ var: 变量名, scope: EFFECT\|BATTLE }` | 读变量 |
| `{ agg: SUM\|COUNT\|MAX\|MIN\|AVG, of: 目标, stat: atk }` | 聚合 |
| `{ event: value\|lethal }` / `{ turn: true }` / `{ random: true }` | 事件数值 / 回合数 / 随机数 |
| `{ if: 条件, then: 数值, else: 数值 }` | 二选一取值（省略 `else` 得 0） |

公式是在**保存卡牌时**编译的（`引擎/值.ts`）：写错会当场报错并指出位置，不会等到结算那一刻才炸。
写错公式的卡与「机读效果写错了」一样：卡还能用，只是没有机读效果。

## 变量与多步决策

`SET_VAR` 赋值、`ADD_VAR` 累加，读的时候写 `$变量名`（变量名用中文也行）：

```yaml
operations:
  - type: SET_VAR
    var: 倒下
    value: 0
  - type: ADD_VAR
    var: 倒下
    value: 1
  - type: MODIFY
    target: SELF
    stat: atk
    value: "$倒下 * 100"
```

两种作用域：

| `scope` | 存活范围 | 用途 |
| --- | --- | --- |
| `EFFECT`（默认） | 本次效果结算期间，**包含它触发的连锁**；结算完自动消失 | 「先打一下，看死没死，再决定要不要弃牌」这类中间结果 |
| `BATTLE` | 整场战斗，随局面快照一起存进聊天变量 | 跨回合记忆：倒了几个、攒了几层 |

⚠️ 效果局部变量不会漏到外面（效果结束就没了），也不影响同一张卡的其他效果；
子效果（这条效果触发出来的连锁）读得到父效果的变量，但自己写的时候是写在自己那一层。
用 `{ var: 变量名 }` 条件判断时默认也是 `EFFECT`，要读战斗级的写 `scope: BATTLE`。

## 控制流：IF / FOR_EACH / REPEAT

| `type` | 参数 | 含义 |
| --- | --- | --- |
| `IF` | `condition`、`then`、`else`（可省略，省略 = 什么都不做） | 二选一 |
| `FOR_EACH` | `target`、`operations`、`max`（默认 32）、`break_if`（可选） | 对每个目标跑一遍循环体 |
| `REPEAT` | `times`（数值）、`operations`、`break_if`（可选） | 重复 `times` 次 |
| `BREAK` | — | 跳出**最内层**循环（不在循环里时只记一条警告） |
| `STOP` | — | 停掉这条效果剩下的操作 |

循环体里能用的两个读数：

- `LOOP_ITEM`：当前这一轮处理的目标（`FOR_EACH`），可以当 `target` 用，也可以写 `LOOP_ITEM.hp`
- `$index`：当前下标，从 0 开始（`FOR_EACH`）；`$round`：当前第几轮，从 0 开始（`REPEAT`）

提前收手有两种写法：`break_if` 写在循环上（条件成立就跳出），或者循环体里的 `BREAK`。

```yaml
# 对敌方每张卡打「当前攻击力的一半」, 打倒两张就收手
- type: SET_VAR
  var: 倒下
  value: 0
- type: FOR_EACH
  target: ALL_ENEMIES
  max: 3
  operations:
    - type: DAMAGE
      target: LOOP_ITEM
      value: "SELF.atk / 2"
    - type: IF
      condition: { target: { of: LOOP_ITEM, zone: FIELD } }   # 打完它还在场上吗
      then: []
      else:
        - type: ADD_VAR
          var: 倒下
          value: 1
  break_if: { var: 倒下, op: ">=", value: 2 }
```

**上限（引擎会自动截断，写超了也不会卡死界面）**：`FOR_EACH` 单次最多 32 个目标、
`REPEAT` 最多 64 轮、循环最多套 4 层、单条效果总共最多 800 个操作。
超过上限时记一条警告，剩下的部分跳过 —— 这属于兜底，正常卡面用不到。

## 条件 (`condition`)

可以任意嵌套组合：

```yaml
all: [ ... ]        # 全部满足
any: [ ... ]        # 任一满足
not: { ... }        # 取反
```

叶子条件：

| 写法 | 含义 |
| --- | --- |
| `exists: {查询}` | 存在满足查询的卡 |
| `count: {查询}, op: ">=", value: 2` | 满足查询的卡数量比较 |
| `self: {...}` | 对来源卡自身的查询 |
| `event: {...}` | 对当前事件的查询 |
| `flag: 键, op: ">=", value: 1` | 来源卡上的标记（`SET_FLAG` 写入） |
| `var: 变量名, op: ">=", value: 1[, scope: EFFECT\|BATTLE]` | 变量比较（默认 `EFFECT`，见 [变量与多步决策](#变量与多步决策)） |
| `target: {...}` | 对某个目标现状的查询（见下） |
| `chance: 0.3` | 按种子随机的 30% 概率 |
| `player: {…}` | 玩家状态查询（见下） |

上面所有 `value` / `count.value` 都可以写公式（见 [数值：常量、公式与聚合](#数值常量公式与聚合)），
例如 `count: { zone: FIELD, controller: OPPONENT }, op: ">=", value: "COUNT(ALL_ENEMIES)"`。

`self` 支持的字段：`zone`、`hp_below`、`hp_percent_below`（0-1）、`atk_above`、
`turns_in_zone`（进入的那回合算 0）、`has_status`、`stat` + `op` + `value`。

⚠️ 这些字段看的都是**经过所有加成/削弱之后**的数值（`current.*`），
`hp_percent_below` 除以的也是**加成后的** `hp_max`。所以「生命低于上限三成」不要写死数字，
要写成公式让阈值跟着上限走：

```yaml
modifiers:
  - stat: atk
    value: 400
    condition: { self: { stat: hp, op: "<=", value: "SELF.hp_max * 0.3" } }   # 上限被削到 800 → 阈值跟着变成 240
```

`event` 支持的字段：`value_above`、`value_below`、`lethal`（该次伤害是否致命）、
`target_controller`（`SELF` / `OPPONENT`）。

`player` 支持的字段：

```yaml
condition:
  player:
    controller: SELF          # SELF（默认）/ OPPONENT
    hp_below: 2000            # 生命值低于绝对值
    hp_percent_below: 0.5     # 生命值低于比例（0-1）
    stat: hp                  # hp / hp_max
    op: ">="
    value: 4000
```

比较运算符：`>`、`>=`、`=`、`<=`、`<`。**YAML 里 `>` 开头的值要加引号**：`op: ">="`。

`target` 用来问「某个目标现在怎样」（例如「刚打的那只还在场上吗」「场上还有两名敌人吗」）：

```yaml
condition:
  target:
    of: ALL_ENEMIES       # 省略 = SELF；可以写任意目标关键字或 { id }
    exists: true         # 是否存在（默认 true；写 false 就是「一个都没有」）
    zone: FIELD
    hp_below: 300
    hp_percent_below: 0.5
    atk_above: 500
    turns_in_zone: 2
    has_status: 灼烧
    stat: atk            # atk / shield_max / shield / hp_max / hp
    op: ">="
    value: 400
    count_op: ">="       # 数符合条件的个数（与上面的单个目标查询可以一起用）
    count_value: 1
```

## 操作 (`operations` / `cost`)

| `type` | 主要参数 | 说明 |
| --- | --- | --- |
| `DAMAGE` | `target`（默认 `EVENT_TARGET`）、`value`（省略则用事件数值）、`pierce` | 走 `BEFORE_DAMAGE` / `AFTER_DAMAGE`，可被取消；**默认先扣护盾**，`pierce: true` 则无视护盾直接扣生命 |
| `HEAL` | `target`（默认 `EVENT_TARGET`）、`value` | 走 `BEFORE_HEAL` / `AFTER_HEAL` |
| `RESTORE_SHIELD` | `target`（默认 `SELF`）、`value` | 回复护盾（不会超过护盾上限）|
| `CLEAR_SHIELD` | `target`（默认 `EVENT_TARGET`） | 把护盾直接清零，等同被打空，会派发 `SHIELD_BROKEN` |
| `DESTROY` | `target`（默认 `EVENT_TARGET`） | 走 `BEFORE_DESTROY`，可被取消 |
| `MOVE` | `target`、`zone`（默认 `GRAVEYARD`） | 直接移动区域，不经过破坏流程 |
| `SUMMON` | `card`（卡牌库 id 或卡名）、`value`（数量）、`data.owner: OPPONENT` | 新造实例并进场 |
| `DRAW` | `value`（张数）、`data.player: OPPONENT` | 抽牌，会派发 `ON_DRAW`；牌库空时按配置轮换墓地 |
| `ATTACK` | `target`（默认 `EVENT_TARGET`）、`pierce` | 让**效果来源卡**攻击目标（每回合 1 次，与手动攻击共用次数） |
| `MODIFY` | `target`（默认 `SELF`）、`stat`、`layer`、`value`、`duration` | 加一条修正；`stat: shield` 直接增减护盾池，`stat: hp` 直接治疗/扣血 |
| `TRANSFER_MAX` | `from`（默认 `SELF`）、`to`（默认 `EVENT_TARGET`）、`stat`（默认 `hp_max`）、`value` | 从一方扣、给另一方加；`stat: shield` 搬运的是护盾池里的现有值 |
| `APPLY_STATUS` | `target`、`status`（同类叠加的键）、`name`、`value`（层数）、`duration`、`modifiers`、`effects` | 附加状态，**同名状态会叠加**（见下） |
| `REMOVE_STATUS` | `target`、`status`（键） | 移除该键的状态 |
| `ADD_EFFECT` | `target`（默认 `SELF`）、`effects` | 给卡临时加一条触发效果 |
| `CANCEL` | — | 取消当前事件 |
| `SCALE_EVENT` | `value`（倍率） | 放大/缩小当前事件的数值 |
| `SET_FLAG` | `target`、`data: { 键: 数值 }` | 写标记，配合 `condition.flag` 用 |
| `SET_VAR` | `var`（变量名）、`value`（数值）、`scope`（默认 `EFFECT`） | 写变量 |
| `ADD_VAR` | 同上 | 在旧值上累加 |
| `IF` | `condition`、`then`、`else` | 按条件二选一（见 [控制流](#控制流-if--for_each--repeat)） |
| `FOR_EACH` | `target`、`operations`、`max`（默认 32）、`break_if` | 对每个目标跑一遍循环体 |
| `REPEAT` | `times`（数值）、`operations`、`break_if` | 重复 `times` 次 |
| `BREAK` / `STOP` | — | 跳出循环 / 停掉这条效果剩下的操作 |

上面写 `value` 的地方都能用公式（见 [数值：常量、公式与聚合](#数值常量公式与聚合)），
比如 `value: "SELF.atk / 2"`。

每个操作都可以带 `condition`，不满足就跳过。

`SUMMON` 之外的操作默认目标见上表；`cost` 里的操作返回值决定「代价是否付得起」，
任一失败则整条效果不发动，且不会留下副作用。

### 状态叠加

`APPLY_STATUS` 遇到目标身上已有同 `status` 键的状态时，不会新建，而是把层数累加：

```yaml
# 每发动一次 +100 攻击力，可以反复叠加
- type: APPLY_STATUS
  target: SELF
  status: 战意
  name: 战意
  duration: { turns: 2, tick_owner: ANY }
  modifiers:
    - stat: atk
      value: 100
```

- `value` 是一次叠加的层数，默认 1
- 叠加后状态携带的 `modifiers` 会**按层数放大**：3 层 × `value: 100` = +300
- 重复施加会刷新 `duration`（按回合数算；`turns: 2` 能活过接下来两次回合结算）
- 层数写在状态上，状态到期时所有层一起消失
- 想知道某个 +100 是哪张卡给的，看战斗面板 → 卡牌详情里的「加成来源」（见下文
  [加成 / 减值的来源](#加成--减值的来源)）

## 护盾（`shield`）

卡面的护盾不是只用来展示的「防御力」，它是一段**会先被打掉的临时生命**。

| 概念 | 说明 |
| --- | --- |
| `shield`（卡面值） | 基础护盾，决定开局时护盾池多大 |
| `shield_max`（当前上限） | 由 `shield` 加上各种修正算出来（走 `SET → ADD → PERCENT_ADD → PERCENT_MULT → OVERRIDE`） |
| 护盾池（当前值） | 实际能挡多少伤害，始终夹在 `0 ~ shield_max` 之间 |

伤害怎么算：

1. 默认**先扣护盾**，护盾不够挡的部分（溢出）才扣生命；
2. `pierce: true` 的伤害（`DAMAGE` / `ATTACK` 都支持）**无视护盾直接扣生命**，护盾一点不掉；
3. 护盾被扣到 0 时派发一次 `SHIELD_BROKEN`：
   - 事件主角就是**护盾被打空的那张卡**（所以 `by: SELF` = 「本卡破盾时」），打破它的一方在 `source` / `data.attacker` 里；
   - 事件的 `value` 是被护盾吃掉的伤害量；
   - 这是**边沿触发**，而不是「护盾为 0 就一直触发」：重新生成护盾后再被打空会再触发一次。

护盾**不会自动回满**：换区域、回合结束都不补盾（与生命值一样是持续状态）。
要回盾就显式写 `RESTORE_SHIELD`、给 `shield` 加修正，或直接改 `shield_max`；
`CLEAR_SHIELD` / `MODIFY stat: shield` 把护盾打到 0 同样算破盾，会派发 `SHIELD_BROKEN`。

```yaml
effects:
  # 回合结束时给自己回 150 点护盾
  - id: mend
    on: TURN_END
    when: CONTROLLER
    operations:
      - type: RESTORE_SHIELD
        target: SELF
        value: 150

  # 破盾时永久 +300 攻击力 (回满盾后再次破盾会再触发)
  - id: shatter
    on: SHIELD_BROKEN
    operations:
      - type: MODIFY
        target: SELF
        stat: atk
        layer: ADD
        value: 300

  # 无视护盾的真伤
  - id: true_strike
    on: AFTER_ATTACK
    operations:
      - type: DAMAGE
        target: EVENT_TARGET
        value: 200
        pierce: true
```

⚠️ 玩家（不是卡）没有护盾，对玩家的伤害直接扣生命。

## 战斗配置（不属于卡牌 YAML）

`createBattle` 的 `BattleConfig` 控制整场战斗的规则：

| 字段 | 默认 | 说明 |
| --- | --- | --- |
| `player_hp` / `enemy_hp` | 8000 | 双方生命上限 |
| `field_size` | 5 | 每方场上最多几张卡 |
| `hand_limit` | 8 | 每方手牌上限，超了就开一条弃牌询问（0 = 不限制）|
| `opening_hand` | 0 | 开局抽牌数 |
| `energy` | 见下 | 能量（上场费用）规则，`{ enabled, start, per_turn, cap, refill }` |
| `recycle` | `GRAVEYARD` | 牌库抽空时把墓地洗回牌库并派发 `ON_RECYCLE`；设成 `NONE` 则抽空就是抽不到 |
| `seed` | — | 随机种子，保证同种子同结果 |

玩家操作的对外接口：`canPlayCard` / `playCard` / `canAttack` / `attack` / `drawCards` /
`listActivatable` / `activate` / `listAsks` / `resolveAsk` / `endSide`（结束自己这一半行动，交给对手）/ `endTurn`（直接推完整个回合）。
战斗结束后（`state.finished`）这些都会拒绝执行。

## 能量（上场费用）

**卡牌的 `能量` 字段 = 这张卡上场要花多少能量**（卡牌库里的一个普通字段，与 ATK / 护盾 / HP 并列；
留空或 `0` 就是不花钱）。名字故意取通用的「能量」，因为这套脚本会被不同角色卡共用，
换成金币 / 法力 / 费用什么的对引擎来说都是同一个东西。

```yaml
# 卡牌库字段示例
名称: 燎原
能量: "4"        # 上场消耗 4 点能量
ATK: "900"
```

规则：

| 配置 | 默认 | 含义 |
| --- | --- | --- |
| `enabled` | `true` | 关掉之后 `能量` 字段不生效，所有卡都能直接上场 |
| `start` | 1 | 第 1 回合的能量上限 |
| `per_turn` | 1 | 每过一个回合上限 +1 |
| `cap` | 10 | 上限的上限 |
| `refill` | `true` | 每次轮到自己行动时把能量补满（关掉则只在回合里累加）|

- 上限公式：`min(cap, start + (turn - 1) × per_turn)`，所以第 1 回合只能上 1 费的卡，
  第 4 回合才能上 4 费的「燎原」；
- **只有轮到自己的那半边行动时才补能量**，一个回合里的两边各自算各自的；
- 付不起的手牌在面板上会变暗、禁止拖出，简报里写 `能量不足 (需 N, 只剩 M)`；
- 上场日志会写 `支付 N 点能量上场「X」(剩余 e/m)`。

能量目前只在**上场**时消耗，攻击与发动效果不花钱。

## 修正层与持续时间

修正按层结算，顺序固定（同层内可交换，所以结果可复现）：

```
SET → ADD → PERCENT_ADD → PERCENT_MULT → OVERRIDE
```

- `layer` 默认 `ADD`
- `PERCENT_ADD` 的 `value: 0.2` 表示 +20%；`PERCENT_MULT` 的 `0.2` 表示 ×1.2
- 可修正的 `stat`：`atk`、`shield_max`、`shield`、`hp_max`、`hp`
- `hp` 是当前生命值，引擎直接读存储值，不走层叠
- `shield` 是当前护盾，同样直接读存储值；`shield_max` 走层叠（就是卡面的护盾值）

持续时间：

```yaml
duration:
  turns: 3                 # 持续 3 个回合
  tick_on: TURN_END        # TURN_START / TURN_END，默认 TURN_END
```

⚠️ `turns` 以**回合**计数（双方各行动一次算一回合），不是「某一边行动一次」。
一个 `turns: 2` 的持续伤害会在接下来 2 次**回合结算**时各扣一次，中途换边不算。

⚠️ `tick_on` 决定在回合结算的哪一头流逝（默认 `TURN_END`）：`TURN_END` 是「先结算持续伤害，再流逝寿命」，
`TURN_START` 是「新回合开始前先流逝寿命」。
同一回合里的先后顺序：`TURN_END`（结算持续伤害）→ 流逝寿命 → 回合号 +1 → `TURN_START`（先流逝寿命再触发 `TURN_START` 效果）。
所以「同一个回合里先扣血再结束」的写法要 `turns` 多留一次。

> 老卡里可能还写着 `tick_owner`：引擎为了兼容旧卡仍然能解析它，但**已经不再生效**，
> 请直接删掉（新写法里持续时间只看 `turns` 与 `tick_on`）。

⚠️ `modifiers` 里的 `target` 在效果物化时**只解析一次**：写 `ALL_ALLIES` 这类目标不会自动跟随之后
的场上变化（例如后进场的卡不会被算进去）。需要动态跟随就写在 `condition` 里。

⚠️ 同理，`modifiers` 里的公式（`value: "SELF.atk / 2"`）也是在**这条修正被创建时**算一次就固定下来
（常驻效果就是卡上场那一刻），之后不会跟着当前数值变。要「每回合重算」的效果，
用 `on: TURN_START` + `ADD_EFFECT` 每回合重铺一次修正。

⚠️ 但修正自带的 `condition` 是**每次重算都重新判定**的，判定的对象就是**加成后的数值** ——
所以「生命低于上限三成」这类阈值会跟着 `hp_max` 的加成/削弱自动收紧或放宽（见上文条件一节）。
为了让条件第一时间反应，引擎在**任何**动到池值或上限的操作之后都会立刻重算，
不用等回合结算：掉血 / 治疗 / 护盾增减 / `RESTORE_SHIELD` / `CLEAR_SHIELD` / `MODIFY` /
`TRANSFER_MAX` / 卡进场上或离场。

重算会**反复跑到数值不再变为止**（最多 4 轮，`引擎/modifiers.ts` 的 `RECALC_PASS_LIMIT`），
所以「条件依赖派生值、派生值又被条件改」的两三层链条能一次算到位；
如果两条修正互相引用（A 的条件看 B，B 的条件看 A），它会来回摆动，引擎跑到 4 轮就收手，
**结果确定、不会死循环**。因此不要写「自己看自己」的条件
（例如「攻击力低于 500 时 +400」——加了就超过 500，条件立刻不成立），
要写就保证条件不会被这条修自己改动的数值推翻。

## 完整示例

见 `src/卡牌系统/卡牌/机读效果示例.ts`（编辑器「插入示例」用的就是它）：

```yaml
effects:
  # 常驻修正: 己方墓地有 2 张以上「炎龙」时攻击力 +20%
  - id: dragon_roar
    modifiers:
      - stat: atk
        layer: PERCENT_ADD
        value: 0.2
        condition:
          count: { zone: GRAVEYARD, controller: SELF, series: 炎龙 }
          op: ">="
          value: 2

  # 攻击后附加「灼烧」: 3 个回合内攻击力 -200
  - id: burn
    on: AFTER_ATTACK
    operations:
      - type: APPLY_STATUS
        target: EVENT_TARGET
        status: 灼烧
        duration: { turns: 3 }
        modifiers:
          - stat: atk
            value: -200

  # 回合结束支付自身召唤同伴
  - id: evolve
    on: TURN_END
    when: CONTROLLER
    condition:
      self: { turns_in_zone: 1 }
    cost:
      - type: DESTROY
        target: SELF
    operations:
      - type: SUMMON
        card: 炎龙之魂

  # 反应型 (陷阱): 我方卡将受到致命伤害时, 支付自身取消这次伤害
  # by 省略的话只认本卡自己造成的伤害, 所以陷阱必须写 by: ANY
  - id: guard
    on: BEFORE_DAMAGE
    by: ANY
    condition:
      all:
        - event: { lethal: true, target_controller: SELF }
        - self: { zone: FIELD }
    cost:
      - type: MOVE
        target: SELF
        zone: GRAVEYARD
    operations:
      - type: CANCEL

  # 穿盾: 攻击后无视护盾追加 100 点真实伤害
  - id: pierce
    on: AFTER_ATTACK
    operations:
      - type: DAMAGE
        target: EVENT_TARGET
        value: 100
        pierce: true

  # 护盾: 破盾时永久 +200 攻击力, 同时给自己回 150 点护盾
  - id: bulwark
    on: SHIELD_BROKEN
    operations:
      - type: MODIFY
        target: SELF
        stat: atk
        value: 200
      - type: RESTORE_SHIELD
        target: SELF
        value: 150

  # 值表达式 + 控制流: 对敌方每张卡打「当前攻击力的一半」, 打倒两张就收手
  - id: sweep
    on: MANUAL
    limit: { per: TURN, times: 1 }
    operations:
      - type: SET_VAR
        var: down
        value: 0
      - type: FOR_EACH
        target: ALL_ENEMIES
        max: 3
        operations:
          - type: DAMAGE
            target: LOOP_ITEM          # 循环里「当前这一张」
            value: "SELF.atk / 2"
          # 多步决策: 打完之后再看它还在不在场上, 决定要不要补刀
          - type: IF
            condition: { target: { of: LOOP_ITEM, zone: FIELD } }
            then:
              - type: DAMAGE
                target: LOOP_ITEM
                value: { op: MUL, args: [{ stat: atk, of: SELF }, 0.25] }
            else:
              - type: ADD_VAR
                var: down
                value: 1
        break_if: { var: down, op: ">=", value: 2 }   # 打倒两张就提前跳出
```

（上面就是编辑器「插入示例」里的全部 7 条，改 `机读效果示例.ts` 时记得同步这里）

再补两条不同风格的例子：

```yaml
# 玩家主动发动：每回合一次，抽两张
- id: study
  on: MANUAL
  limit: { per: TURN, times: 1 }
  operations:
    - type: DRAW
      value: 2

# 回合结束自动攻击对方血最少的卡
- id: berserk
  on: TURN_END
  when: CONTROLLER
  operations:
    - type: ATTACK
      target: { zone: FIELD, controller: OPPONENT, max: 1, sort: HP_ASC }
```

## 观看战斗：日志分级与加成来源

战斗面板 → `日志` 弹窗把三种记录合在一起（操作记录 / 引擎日志 / AI 决策），上面一排标签可以多选着筛：

| 分组 | 标签 | 含义 |
| --- | --- | --- |
| — | 全部 | 不按标签筛（清空标签也是这个效果） |
| 来源 | 操作 / AI决策 | 玩家操作记录、AI 的每个决策回合 |
| 等级 | 调试 / 信息 / 警告 / 错误 | 每一步的内部细节 / 日常流水 / 需要你看一眼的 / 出错了 |
| 类别 | 战斗 / 效果 / 区域 / 状态 / 数值 / 系统 / 事件 | 这条记录来自引擎的哪一块 |

等级从低到高是 `DEBUG` → `INFO` → `WARN` → `ERROR`；列表里 `警告` / `错误` 的行会带一个彩色小标签。

筛选规则：

- **引擎把全部等级都记了下来**，只是默认只展示 `信息` 及以上（默认选中「信息 / 警告 / 错误」三个标签）：
  把 `调试` 也选上，就能回看之前发生的每一步细节。
- 标签之间可以切换组合方式：`任一`（OR，命中任意一个标签）/ `同时`（AND，必须同时满足，比如「警告 + 系统」）；
  一个标签都不选 = 不按标签筛。
- 搜索框匹配正文与类别名（空格分隔的多个词需要全部命中）；搜索与标签叠加时始终取交集。
- 引擎内部问题（卡牌库缺卡等）会多带一个 `内部` 小标签。

给 AI 的简报是另一回事：

- 「最近战况」只带 `INFO` 以上，不会把 `DEBUG` 的事件流水与抽牌流水塞进上下文。
- **引擎内部的麻烦不会进简报**：卡牌库里找不到卡、事件连锁过深、事件队列异常，AI 也修不了，塞进上下文只是噪音。
- **与操作相关的警告照常进简报**：`场上已满`、`无效的攻击目标`，让 AI 知道自己那一步为什么没成。

日志本身也有体积上限：超过上限后从最旧的 `DEBUG` 丢起（不够再丢最旧的 `INFO`），`WARN` / `ERROR` 永远保留；
写进聊天变量的局面快照只带最近一段日志，内存里的完整日志不受影响。

### 加成 / 减值的来源

面板上写 `ATK 500 (+200)`，光看数字不知道这 200 是谁给的。所以引擎把每条修正都记下了来源：

- **卡牌详情**里有一栏「加成来源」，逐条列出生效的与没生效的：
  `+200 ← 「愿之芽」`、`+100 ×2层 (2T) ← 「战意」 · 战意`、`+20% ← 「炎龙」`，
  来源是引擎/战斗配置时写 `引擎`；没生效的会补上原因（`(未生效: 条件不满足)` / `(未生效: 来源不在场上)`）。
- **持续状态**后面会写施加者：`战意 (2T) ←「战意」`。
- **给 AI 的简报**同样带来源：`c9 愿之芽 500(+200)/100/800 [可攻击] {ATK +200 ← 「愿之芽」}`，
  这样 AI 也能说清「这 200 是你自己的常驻效果，不是对手给的」。

写卡时不用做任何额外的事：`on` / `id` / 状态名本身就是溯源信息。
一张卡只有一条效果时不会显示效果 id（只写卡名），有多条时才会列出效果 id。

## 机读区写错了会怎样

校验失败**不会让整张卡不可用**：卡牌本身照常显示、照常进卡组，只是没有机读效果。
`createCardProvider` 会把每张卡的错误收集在 `errors[卡牌 id]` 里，便于提示作者。

## 不想学这套语法？让 AI 写

编辑器里有一个 `AI 写机读` 按钮：

1. 你按自己的话把卡写清楚（名称、数值、描述，怎么描述效果都行）。
2. 可选：在「补充说明」里写你想让它怎么处理（例如「这个效果一回合只能用一次」）。
3. 点按钮，AI 会读你填的卡面文字，把它翻译成本文件的 YAML，直接写进机读区。

它做的事和限制：

- 用的是 Tavern 的 `generateRaw`（不带预设、不写聊天记录），只发一张卡的信息，**不会**碰你的聊天上下文或世界书。
- 生成的 YAML 会经过 `parseMachineEffect` 校验；校验不过会让 AI 自己改一次，再不过就报错，不会把坏 YAML 塞进去。
- AI 可能判断这张卡「没有需要机器识别的效果」（纯剧情卡、纯文本卡），这时它不会写机读区，而是告诉你为什么。
- 所以**AI 写完之后最好自己扫一眼**：机读区里出现的数字应该和卡面数值一致。

### 换一个 AI 来写卡

写卡相关的三个功能（AI 写机读 / AI 生成卡牌 / AI 生成卡组）默认都用酒馆当前选中的接口。
如果嫌当前模型贵或不够聪明，可以点写卡界面下方的灰字 `写卡用的 AI: … · 点击更换` 改成：

| 模式 | 说明 |
| --- | --- |
| 当前 | 用酒馆当前接口与模型，不做任何覆盖（默认） |
| 预设 | 用酒馆里已保存的另一个代理预设（地址与密钥都跟预设走） |
| 自定义 | 直接填 OAI 兼容接口的地址 / 密钥 / 接口类型 / 模型 |

- 三种模式都可以额外覆盖「温度」与「回复长度」，留空则用接口默认值。
- 设置存在脚本变量里，与当前对话、角色卡无关，换聊天不会丢。
- 只影响写卡，**不会**改变聊天里那个 AI 的接口。
- 模式选了「预设」或「自定义」但没填完时，写卡会直接报错提醒，不会默默回退到当前接口。

## 卡牌阵营（`阵营`）

每张卡有一个 `阵营` 字段，取值为 `通用` / `我方` / `敌方`（默认 `通用`）：

- `通用` 的卡在两边卡牌库和选卡列表里都能看到。
- `我方` / `敌方` 的卡只在对应阵营的卡组里出现，方便把敌方卡对玩家隐藏。
- 卡组也有 `阵营`（`我方` / `敌方`）。战斗面板的「我方卡组」只列我方卡组，「敌方卡组」只列敌方卡组。

## 批量生成卡牌（「卡组」面板 → `AI 生成卡组`）

一次生成一整套卡比一张张写快得多。弹窗里可以选两种方式：

| 方式 | 行为 |
| --- | --- |
| 新建卡组 | 生成一整套新卡，自动建成一个新卡组；可指定一张对手卡组当强度参考（也可以「不参考」） |
| 追加到已有卡组 | 生成的新卡直接加进你指定的卡组；**这套卡组里已有的卡会一起发给 AI**，让它补齐短板而不是重复定位 |

- 生成的卡按目标卡组的阵营入库（`我方` / `敌方`）。
- 张数超过 10 张时弹窗会提示：卡太多建议换用逻辑清晰、分析能力强的模型，否则可能输出混乱或质量下降。

### 聊天上下文开关

三个写卡功能都会在弹窗里显示一个「带入最近聊天记录」开关：

- 打开后可以自己填条数（1~200），默认：AI 写机读不带（只发卡面文字），AI 生成卡牌不带，AI 生成卡组带 30 条。
- 不带上下文时只按卡牌信息生成，适合「我要一张很酷的卡」这种跟剧情无关的需求。
- 打开后生成内容会更贴合当前剧情，但也会把聊天记录发给模型。

## 数据不完整的卡会被灰显

卡牌可以只填一半就保存（比如先建好再慢慢补）。这种卡不会失效，但界面会提醒：

- **卡牌库列表**：不完整的卡用灰底 + 虚线边框标出，右上角有 `数据不完整` 小标签，悬停能看到具体缺什么。
- **卡组详情**：同样的灰虚线标记；卡名那一行会写明 `不在卡牌库`（卡被删了）或 `数据不完整`（卡还在但没填完）。
- **卡牌编辑**：缺的字段（卡名 / ATK / 护盾 / HP / 机读效果）的输入框变灰 + 虚线边框，悬停显示原因；机读效果写错时会在下面给一行灰字说明。

这些标记只是提示，不影响保存、导出和战斗中的使用。

## 维护本文件

引擎语法变化时，按这个顺序改，保证文档不会和代码脱节：

1. **`src/卡牌系统/引擎/schema.ts`** —— 唯一真实来源。新增/修改字段先改这里；
   数值公式（字符串糖、聚合、变量）的语法在 `src/卡牌系统/引擎/值.ts`，
   求值在 `conditions.ts`，上限常量在 `operations.ts`。
2. **本文件** —— 同步表格与说明（字段、时点、目标、条件、操作）。
3. **`src/卡牌系统/AI/规范.ts`** —— AI 写机读时喂给模型的语法说明，必须和本文件保持一致。
4. **`src/卡牌系统/卡牌/机读效果示例.ts`** —— 若新语法值得展示，更新编辑器「插入示例」的内容。
5. **`src/卡牌系统/引擎/测试卡.ts`** —— 加一张覆盖新机制的测试卡。
6. **`src/卡牌系统/引擎/测试.ts`** —— 加断言，然后跑：

```powershell
node src/卡牌系统/引擎/测试.ts
corepack pnpm exec eslint "src/卡牌系统/**/*.{ts,vue}"
```

第 5 步会顺带校验「插入示例」的 YAML 是否仍然合法（`测试.ts` 里有一项断言专门跑它），
所以只要测试全绿，示例与文档就不会落后于引擎。

7. **如果改的是卡牌字段或战斗规则（不是机读语法）** —— 除了 `引擎/schema.ts`，还要一路同步：
   `卡牌/schema.ts` + `卡牌/校验.ts` + `卡牌/去重.ts` + `卡牌/components/CardEditor.vue` / `CardView.vue`（能填、能显示、能查重）、
   `战斗/schema.ts`（面板设置项）、`战斗/简报.ts` + `战斗/决策.ts` + `战斗/协议.ts`（AI 必须知道这件事；
   协议内容变了要 `PROTOCOL_VERSION` +1 并重新运行 `node src/卡牌系统/战斗/生成世界书.ts`）、
   以及本机维护的引擎 / 卡牌库开发笔记。

> 引擎内部的实现细节（模块划分、事件结算、修正层叠等）见本机的引擎开发笔记，本文件只讲**怎么写机读效果**。
