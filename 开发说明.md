# 开发说明 · 卡牌战斗系统

酒馆助手 (Tavern Helper) 脚本「卡牌库 / 卡组 / 卡牌战斗」的独立代码库。

面向使用者的功能介绍见 [`README.md`](./README.md); 本文件讲**怎么开发、有什么约束**。

## 目录布局

本项目与参考模板仓库**并列存在**, 而不是它的子目录:

```
d:\Downloads\work\
├─ card-battle\             ← 本项目 (本文件所在处)
└─ tavern_helper_template\  ← 参考模板 (StageDog/tavern_helper_template), 保持干净, 可随时 git pull
```

## 与原模板的依赖关系

本项目**不复制也不打包**原模板的任何代码, 只在编译期读它的类型定义:

| 复用的东西 | 引用方式 | 位置 |
| --- | --- | --- |
| `@types/**` 酒馆助手 / MVU / 酒馆本体的全局类型 | `tsconfig.json` 的 `include` | `../tavern_helper_template/@types` |
| 酒馆助手接口的用法说明 | 直接阅读 | `../tavern_helper_template/AGENTS.md` |
| webpack 配置 / `script_bundle.ts` | 照抄思路后自己维护 | 本项目根目录 |

结论:

- 本项目**不修改原模板的任何文件**, 所以原模板的 git 工作区始终干净, 可随时 `git pull`
- **本项目必须和 `tavern_helper_template` 放在同一个父目录下**, 否则编译时找不到类型
- 类型定义只有编译期参与, 不进产物; **不要把 `@types` 或 `util` 复制进本仓库**
  (那就变成分发了, 见下面「许可与依赖边界」)

### 曾经的 `util/*` 依赖已全部自实现

原先用到模板 `util/script.ts`、`util/common.ts` 的 6 个函数, 现在都在 `src/卡牌系统/共用/平台.ts`
里基于酒馆助手公开接口重新实现: `createScriptIdDiv`、`teleportStyle`、`reloadOnChatChange`、
`uuidv4`、`literalYamlify`、`parseString`。

改动这些函数前请先读懂下面的「许可与依赖边界」。

## 许可与依赖边界

- 本仓库以 **GPL-3.0-only** 发布 (见 `LICENSE`、`THIRD-PARTY-NOTICES.md`)
- 参考模板 `tavern_helper_template` 用的是 **Aladdin Free Public License (AFPL)**: 它自己声明
  「不是开源许可证」、要求衍生作品**整体**以 AFPL 授权、且禁止涉及付款的分发
- AFPL 与 GPL-3.0 **无法兼容** (GPL-3.0 不允许对下游加额外限制, 而 AFPL 禁止收费分发),
  所以下面三条是硬约束:

1. **不把模板代码打进产物**: 远程 import 模板的 `.ts` / 内联它的实现都算把 AFPL 代码带进产物
2. **不把模板文件复制进本仓库** (`@types`、`util`、示例、初始模板都不行)
3. 需要新工具函数时, 写在 `src/卡牌系统/` 里、只依据 `@types` 中的公开接口自己实现

模板自身的 LICENSE 还有一层瑕疵: 它照抄了 AFPL 的 Ghostscript 版原文, §0 仍写着「本许可证
适用于 AFPL Ghostscript」、版权人写的是 Artifex, 未按 AFPL 要求换成自己的作品与版权人。
也就是说它的授权状态本身不清晰 (要么受 AFPL 全部限制, 要么根本没授权), 更不足以放心内联。

> 已向上游提出许可疑问, 若日后 StageDog 把 `@types` / `util` 明确为 MIT 等自由许可,
> 可以再评估是否改为直接引用。

## 构建

```bash
corepack pnpm install        # 首次
corepack pnpm build:dev      # 开发构建 (代码美化, 便于在酒馆里阅读产物)
corepack pnpm build          # 生产构建 (压缩混淆, 见下)
corepack pnpm lint           # eslint
```

产物都在 `dist\` (已被 `.gitignore` 忽略):

- `dist\卡牌系统\index.js` — 打包后的脚本本体
- `dist\卡牌系统\酒馆助手脚本-卡牌系统.json` — **酒馆助手「脚本库」的导入格式**, 在酒馆里点「导入」选中它即可

脚本 id 由 `src\卡牌系统\script.json` 固定, 因此重复导入是覆盖同一个脚本, 不会攒出一堆同名脚本。

### 与原模板构建的差异

- **没有 `pnpm watch` / 酒馆实时同步服务**: 原模板的 webpack 配置里有 socket.io 推送 (端口 6621),
  这里已移除。想恢复就照抄 `../tavern_helper_template/webpack.config.ts` 的 `watch_tavern_helper`,
  并把 `socket.io` 加回 `package.json` (它已经在依赖里了)。
- **没有 `tavern_sync`**: 世界书/角色卡的打包由本项目自己的生成脚本负责 (见下)。
- **只扫描 `src/**/index.{ts,tsx,js,jsx}`** 作为入口, 不扫描原模板的 `示例` / `初始模板`。
- `dump_schema.ts` 保留但当前是空操作: 它只处理导出名为 `Schema` 的 `src/**/schema.ts`,
  而卡牌系统导出的是 `CardSchema`、`CardLibraryVariableSchema` 等具名 schema, 用不到 YAML Schema 提示。
- 引入了原模板的 `tsconfig` 开关 `allowImportingTsExtensions` / `rewriteRelativeImportExtensions`:
  源码里的相对导入写 `.ts` 后缀, 既能让 `node xxx.ts` 直接跑测试, 又能被 webpack 打包。

## 目录

```
src\卡牌系统\            脚本源码, 唯一入口是 index.ts (在酒馆脚本按钮区加「卡牌库 / 卡组 / 战斗 / 调试」四个面板)
├─ 卡牌\                 卡牌库: 数据读写、校验、去重、编辑与预览界面
├─ 卡组\                 卡组: 聊天变量里的卡组数据、选卡成组、出战时快照到聊天变量
├─ 引擎\                 战斗引擎 (数据驱动, 全部是 BattleState 上的纯函数)
├─ 战斗\                 面板 + AI 交互: 简报、决策、协议、回放、世界书、卡牌详情
├─ AI\                   调酒馆 AI 生成卡牌与卡组 (提示词规范、解析、客户端)
├─ 共用\                 通知、弹窗、外观 (面板透明度/模糊)、数字、平台工具与样式小工具
├─ 调试\                 三个作用域的变量占用与最近通知
└─ 重置.ts               清空卡牌系统在角色卡/聊天里的数据
资源\                    需要手动导入酒馆的东西
├─ 世界书-卡牌战斗.json   (由 战斗\生成世界书.ts 生成, 手动导入酒馆)
├─ AI战斗协议.md          AI 决策用到的协议说明
└─ 正则\                  酒馆正则 (手动导入)
机读效果.md               卡牌「机读效果」(YAML) 的书写语法参考
.agents\                  开发笔记 (卡牌库 / 卡组 / 引擎)
```

## 生成与测试脚本

```bash
# 重新生成世界书 (改动 PROTOCOL_VERSION 或协议条目后必做, 然后重新导入酒馆覆盖)
node src\卡牌系统\战斗\生成世界书.ts        # 输出 资源\世界书-卡牌战斗.json

# 纯逻辑测试, 不依赖酒馆网页 (node 24 直接执行 .ts)
corepack pnpm test:common    # 52 项
corepack pnpm test:engine    # 316 项
corepack pnpm test:card      # 27 项
corepack pnpm test:ai        # 157 项
corepack pnpm test:battle    # 284 项
```

改了引擎、协议、提示词规范后应当跑一遍这些测试再打包。

## 编写须遵守的既有约定

- 卡牌行为的真实来源是 `src\卡牌系统\引擎\schema.ts`, 书写语法见 `机读效果.md`, 改效果字段前先读它
- 引擎只认 `BattleState` 上的纯函数; 面板层与 AI 层负责把它接到聊天变量上
- **不要新增 `src\*\index.ts`**: webpack 会把每个 `src/**/index.ts` 当成一个独立脚本入口,
  公共代码请用别的文件名
- 脚本运行在隐藏的 0×0 iframe 里, 面板 DOM 在主文档: 点击外部、创建元素、测量视口都要用
  `window.parent` / `createScriptIdDiv()`, 细节见 `.agents\卡牌库笔记.md`
- 界面文案不暴露实现细节 (不写「角色卡变量的命名空间」这类内部信息)

## 酒馆助手接口

本项目可直接使用 `@types` 里定义的所有酒馆助手接口与全局变量 (`getChatMessages`、`generateRaw`、
`triggerSlash`、`$`、`_`、`toastr`、`YAML`、`z` …), 无需 import 也无需检查可用性。
这些接口的清单与用法见 `../tavern_helper_template/AGENTS.md` 的「项目参考文件」一节。
